# 设置端口
server:
  port: 8080

# 设置数据源
spring:
  datasource:
    url: jdbc:mysql://129.211.68.243:3306/mysql_test?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowMultiQueries=true&nullCatalogMeansCurrent=true&allowPublicKeyRetrieval=true
    username: developer
    password: dev123456
    # 驱动
    driver-class-name: com.mysql.cj.jdbc.Driver

# 配置mybatis
mybatis:
  mapper-locations: classpath:mappers/*.xml
  # 全局的映射，不用在xml文件写实体类的全路径
  type-aliases-package: com.service.findservice.placeOrder.entity
  configuration:
    # 开启驼峰映射
    map-underscore-to-camel-case: true
