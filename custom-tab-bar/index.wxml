<van-tabbar active="{{ active }}" bind:change="onChange" active-color="#00AA00"
  inactive-color="#000">
  <van-tabbar-item wx:for="{{list}}" wx:key="index" icon="{{item.icon}}">
  {{item.text}}
  </van-tabbar-item>
</van-tabbar>
